# 対応 LaTeX パッケージ 調査レポート

> このファイルは `tests/run-tests.sh` が GitHub Actions（配布イメージの container）上で
> 自動生成・上書きします。これはプレースホルダです。
>
> 最新の結果は **Actions → "Test LaTeX packages" → Artifacts (`latex-package-test`)** の
> `REPORT.md` を参照してください。run の Summary 画面にも同じ内容が表示されます。
