# 対応 LaTeX パッケージ 調査レポート（生成物）

このファイルには**固定のスナップショットを置きません**（環境の更新で古くなるため）。
`tests/run-tests.sh` が GitHub Actions の実行時に最新版を生成します。

**最新の結果はこちら:**

- ワークフロー: <https://github.com/sotetsuk/latex-ja-template/actions/workflows/test-packages.yml>
- 各 run の **Summary** 画面、または **Artifacts (`latex-package-test`)** に含まれる
  `REPORT.md`（パッケージ × エンジンのサポート可否マトリクス）とショーケース PDF を参照してください。

> 判定はすべて**配布イメージ上での実コンパイル結果**です（ローカル環境ではなく）。
